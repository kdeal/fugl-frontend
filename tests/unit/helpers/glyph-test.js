import { glyph } from 'fugl-frontend/helpers/glyph';
import { module, test } from 'qunit';

module('Unit | Helper | glyph');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = glyph([42]);
  assert.ok(result);
});
